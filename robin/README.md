# Robin vNext

Layered local-first research, evidence, factor, experiment, and strategy system.

The package is introduced beside `portfolio_bot` and is consumed through compatibility facades until migration is complete.
